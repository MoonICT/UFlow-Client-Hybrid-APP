/**
 * @create
 * @modify
 * @desc [description]
 */

import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  imgAva: {
    borderRadius: 12,
    maxWidth: '100%',
    height: 190,
    resizeMode: 'cover',
    marginBottom: 16,
  },
  info: {
    // marginBottom: 16,
  },
});
